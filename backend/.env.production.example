# Production Environment Configuration
# This file shows what environment variables you need to set in Cloud Run

# Firebase Configuration
FIREBASE_PROJECT_ID=shelfmates-de7ef
# For Cloud Run, use service account attached to the service instead of JSON file
# FIREBASE_CREDENTIALS_PATH is not needed when using default service account

# Environment
ENVIRONMENT=production
PORT=8000

# CORS - Add your actual Vercel domain
ALLOWED_ORIGINS=["https://your-app.vercel.app","https://your-custom-domain.com"]

# JWT Configuration - CHANGE THESE IN PRODUCTION!
SECRET_KEY=generate-a-secure-random-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
